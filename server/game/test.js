const Turn = require('./turn')

console.log(`start >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>`)
const turn = new Turn()
console.log(turn)
turn.dice.forEach(die => { if (die.pointer) die.held = true })
turn.roll()
turn.dice.forEach(die => { if (die.pointer) die.held = true })
console.log(turn)
turn.dice.forEach(die => { if (die.pointer) die.held = true })
turn.stop()
console.log(turn)